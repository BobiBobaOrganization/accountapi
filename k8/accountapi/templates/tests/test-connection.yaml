apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accountapi.fullname" . }}-test-connection"
  labels:
    {{- include "accountapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "accountapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
